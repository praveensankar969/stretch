name: Deploy website to GitHub Pages

on:
  push:
    branches: [master]
    paths:
      - "website/**"
      - "src/assets/**"
      - "src/shared/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - name: Stage site (website + referenced src assets)
        run: |
          mkdir -p _site/src
          cp -r website/. _site/
          mkdir -p _site/src/assets
          cp -r src/assets/fonts _site/src/assets/fonts
          cp src/assets/fonts.css src/assets/tokens.css _site/src/assets/
          cp -r src/shared _site/src/shared
          touch _site/.nojekyll
      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./_site
      - id: deploy
        uses: actions/deploy-pages@v4